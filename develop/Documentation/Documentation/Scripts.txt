taskkill /f /im ApplicationGenerator.exe

cd D:\MC\RazorViewsDesigner\HydraCLI\HydraCli
npm link
tsc -p ./
tsc -p ..\HydraCli

hydra start sample
hydra generate app
hydra generate app --debug
hydra generate app --skipInstalls
hydra generate app --noFileCreation
hydra delete sample
hydra clean

{
    "servicesProjectPath": "%HYDRASOLUTIONPATH%\\Ripley.Services\\Ripley.Services.csproj",
    "entitiesProjectPath": "%HYDRASOLUTIONPATH%\\Ripley.Entities\\Ripley.Entities.csproj"
	"packageCachePath": "%APPDATA%\\hydra\\cache"
}

ionic start myApp super --type=ionic-angular
npm uninstall ionic -g

****************** TODO *********************

npm install @ionic/app-scripts
npm rebuild node-sass

"devDependencies": {
    "@ionic/app-scripts": "^3.2.1",
    "@types/node": "^10.9.2",
    "ts-loader": "^4.5.0",
    "ts-node": "^7.0.1",
    "typescript": "^2.9.2",

    "webpack": "^4.17.1"
  },


npm install -g karma-cli
npm install --save-dev @types/jasmine@2.5.41 @types/node html-loader jasmine karma karma-webpack ts-loader karma-sourcemap-loader karma-jasmine karma-jasmine-html-reporter angular2-template-loader karma-chrome-launcher null-loader karma-htmlfile-reporter

tsc -v
Version 2.8.4

    "ag-grid@^18.1.2",
    "ag-grid-angular@^18.1.0"

Install everything:

Install NodeJs
	"d:\Users\Ken\Downloads\NodeJs\node-v10.16.3-x64.msi"
Install VSCode
	"d:\Users\Ken\Downloads\VS Code\VSCodeSetup-x64-1.35.1.exe"
	-- todo
	Set up special settings, default terminal location, extensions, etc.
Install TypeScript
	npm install typescript -g

var hydraSolutionPath = Path.GetFullPath(Environment.ExpandEnvironmentVariables("%HYDRASOLUTIONPATH%"));

No longer have to do (doesn't work):
            "webRoot": "${workspaceRoot}/GenOutput/blank/www"
Do this instead
            "webRoot": "${workspaceRoot}/GenOutput/blank"


git clone https://dev.azure.com/cloudideaas/Hydra/_git/Hydra .
nuget restore -PackagesDirectory packages ApplicationGeneratorBuildTasks.sln
msbuild.exe ApplicationGeneratorBuildTasks.sln /nologo  /p:platform="Any CPU" /p:configuration="Release" -verbosity:diagnostic
